<div class="">
  <div class="mx-auto max-w-7xl px-8 py-2 text-xs md:px-12 lg:px-32">
    <div
      x-data="{ open: false }"
      class="relative mx-auto flex w-full flex-col rounded-xl md:flex-row md:items-center md:justify-between md:rounded-full"
    >
      <div class="flex flex-row items-center justify-between md:justify-start">
        <a
          class="inline-flex items-center gap-3 font-normal uppercase text-black hover:text-zinc-500"
          href="/"
        >
          os.studio
        </a>
        <button
          @click="open = !open"
          class="inline-flex items-center justify-center p-2 text-black hover:text-blue-500 focus:text-black focus:outline-none md:hidden"
        >
          <svg
            class="h-6 w-6"
            stroke="currentColor"
            fill="none"
            viewBox="0 0 24 24"
          >
            <path
              :class="{'hidden': open, 'inline-flex': !open }"
              class="inline-flex"
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M4 6h16M4 12h16M4 18h16"></path>
            <path
              :class="{'hidden': !open, 'inline-flex': open }"
              class="hidden"
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M6 18L18 6M6 6l12 12"></path>
          </svg>
        </button>
      </div>
      <nav
        :class="{'flex': open, 'hidden': !open}"
        class="hidden flex-grow flex-col justify-center py-12 md:flex md:flex-row md:items-end md:py-0"
      >
        <ul
          class="list-none items-center justify-center gap-3 space-y-2 text-center md:ml-auto md:inline-flex md:space-y-0 md:text-left"
        >
          <li>
            <a
              href="mailto:jp@flojoy.io"
              class="inline-flex h-8 items-center justify-center rounded-full border border-transparent bg-black px-4 py-1.5 text-white ring-1 ring-transparent duration-200 hover:bg-black/10 hover:text-black focus:ring-2 focus:ring-white focus:ring-offset-2 lg:ml-auto"
            >
              Contact Us
            </a>
          </li>
        </ul>
      </nav>
    </div>
  </div>
</div>
